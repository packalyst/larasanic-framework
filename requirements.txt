# Larasanic Framework Dependencies
# Core dependencies required by the framework

# Web Framework
sanic>=23.0.0
sanic-ext>=23.0.0

# Database ORM & Migrations
tortoise-orm>=0.19.0
aerich>=0.7.0
asyncpg>=0.27.0  # PostgreSQL adapter
aiomysql>=0.1.0  # MySQL adapter (optional)

# HTTP Client
aiohttp>=3.8.0
requests>=2.28.0
certifi>=2022.0.0

# Async Redis (for cache and websocket support)
redis>=4.5.0

# Security & Crypto
bcrypt>=4.0.0
PyJWT>=2.6.0
cryptography>=40.0.0
itsdangerous>=2.1.0

# Environment & Configuration
python-dotenv>=0.21.0

# Template Engine (Blade)
# Note: Using custom blade implementation in framework/support/blade_engine.py

# Utilities
tomlkit>=0.13.0  # For aerich configuration
